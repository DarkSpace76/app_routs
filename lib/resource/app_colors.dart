// ignore_for_file: prefer_const_constructors

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppColors {
  //Vacancy Page
  static const preViewInfo = Color(0xFF6B6B6B);
  static const payment = Color(0xFF38D191);
  static const vacancyCard = Color(0xFFF2F0F0);
  static const menuBg = Color(0xff565656);
  static const LinearGradient newMessage = LinearGradient(
      begin: Alignment.centerLeft,
      end: Alignment.centerRight,
      colors: [Colors.white, Color(0xFFA5D6A7), Color(0xFFA5D6A7)]);
  static const LinearGradient activeChat = LinearGradient(
      begin: Alignment.centerLeft,
      end: Alignment.centerRight,
      colors: [Colors.white, Color(0xFFE3F2FD), Color(0xFFE3F2FD)]);
}
